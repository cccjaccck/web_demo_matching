import styled from "styled-components";

const Wrapper = styled.div`
  width: 100vw;
`;

const Home = () => {
  return (
    <Wrapper>
      <span>Home</span>
    </Wrapper>
  );
};

export default Home;
